name: offline_image_upload  # ✅ Not same as any dependency!
description: "Flutter app to upload images in-memory when offline."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  provider: ^6.1.2
  connectivity_plus: ^5.0.2
  http: ^1.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
